rpm repository at reallyenglish.

echo "%_topdir %(echo $HOME)/reallyenglish-rpm" >> ~/.rpmmacros

> sudo yum install rpm-build redhat-rpm-config make gcc gcc-c++ yum-utils \
 rpmlint

> git clone git@github.com:reallyenglish/reallyenglish-rpm.git

for more information, please see:

http://wiki.reallyenglish.com/?SysAdmin/RPM (intranet only)
